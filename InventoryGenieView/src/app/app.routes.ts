import { Routes } from '@angular/router';
import { RegisterComponent } from './modules/inventory/pages/register/register.component';
export const routes: Routes = [
    {
        path: '',
        component: RegisterComponent
    }
];
